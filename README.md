# 電卓アプリ

四則演算（`+`, `-`, `*`, `/`）と括弧に対応した、シンプルな電卓を実装しました。

## 使い方

```bash
python calculator.py
```

起動後に式を入力すると結果を表示します。

例:

```text
式を入力してください (例: 1 + 2 * 3): (1 + 2) * 4
結果: 12
```

## テスト

```bash
python -m unittest discover -s tests
```
